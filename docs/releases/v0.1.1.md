## 更新内容

- 新增独立的主控与探针一键安装脚本，支持自动识别 amd64、arm64 架构。
- 下载二进制时校验 SHA-256，支持 curl 或 wget。
- 后台添加节点后生成一键安装命令，上报地址使用当前访问后台的地址。
- 支持 HTTP/IP 临时测试及 HTTPS/WSS 上报。
- 重新安装时保留主控数据库和管理员密钥，并更新服务配置。
- 清理旧版探针的地址覆盖文件，避免更新后仍使用旧上报地址。

## 升级注意

- 主控数据库与 v0.1.0 兼容。
- HTTP 会明文传输密钥与监控数据，仅适合临时测试。
